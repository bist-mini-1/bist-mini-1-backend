# 📋 개발 프로세스 & 워크로 가이드

프로젝트 주제 선정부터 병합까지의 전체적인 개발 흐름과 방식을 정의합니다.

---

## 🛠 1. 프로젝트 진행 단계

### 1단계: 주제 및 기능 정의
- **주제 선정**: 팀원 간 협의를 통한 목표 설정
- **기능 분할**: 4개의 독립적인 파트(Parts)로 요구사항 정의

### 2단계: 데이터 및 아키텍처 설계
- **DB 스키마**: ERD 설계 및 테이블 명세서 작성
- **아키텍처**: 도메인 중심의 계층형 아키텍처(예: `도메인` 패키지 하위에 `controller`, `service`, `dao` 등 구성) 적용

---

## 🔄 2. 작업 프로세스 (Workflows)

### 브랜치 전략
- `main`: 직접 Push를 지양하고 보호합니다.
- `feature/[본인닉네임]`: 본인 파트 작업을 위한 개별 브랜치입니다.

### 작업 흐름
1. `main` 브랜치로부터 최신 코드를 pull 받습니다.
2. `feature/닉네임` 브랜치를 생성하여 개발합니다.
3. 작업 완료 후 `main` 브랜치로 **Pull Request(PR)**를 생성합니다.
4. 팀원 리뷰 및 승인(Approve) 후 머지를 진행합니다.

---

## 💡 개발 모드 전환 (Mock vs Real)

DB 연동 여부에 따라 개발 모드를 유연하게 전환할 수 있습니다.

### 1️⃣ Mock 모드 (현재 기본값)
DB 서버 없이 비즈니스 로직을 먼저 개발할 때 사용합니다.
- **특징**: 서비스 레이어에서 하드코딩된 가짜 데이터를 반환합니다.
- **설정**: `BackendApplication.java`에서 `DataSourceAutoConfiguration` 등을 제외(`exclude`) 처리합니다.

### 2️⃣ Real DB 연동 모드
실제 DB와 연동하여 데이터를 처리할 때 사용합니다.
- **전환 방법**:
  1. `BackendApplication.java`에서 어노테이션의 `exclude` 옵션 전체 삭제
  2. `Service` 레이어의 `@Transactional` 및 DB 호출 코드 주석 해제
  3. `application.yml` 및 `.env`에 실제 DB 정보 입력

---

## 🧪 3. 테스트 및 검증
- **Swagger / Postman**: 작성한 API 기능을 수시로 검증합니다.
- **Mock API**: 의존적인 파트가 미완성인 경우 Mock 데이터를 활용하여 독립 테스트를 수행합니다.
