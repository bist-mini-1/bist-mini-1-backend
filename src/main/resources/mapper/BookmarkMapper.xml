<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bist.mini.post.dao.BookmarkDao">

    <select id="countBookmark" resultType="int">
        SELECT COUNT(*)
        FROM bookmarks
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </select>

    <insert id="insertBookmark">
        INSERT INTO bookmarks (post_id, member_id, created_at)
        VALUES (#{postId}, #{memberId}, SYSDATE)
    </insert>

    <delete id="deleteBookmark">
        DELETE FROM bookmarks
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </delete>

</mapper>
