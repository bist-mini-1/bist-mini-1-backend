<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bist.mini.comment.dao.CommentLikeDao">

    <insert id="insertLike" parameterType="com.bist.mini.comment.entity.CommentLike">
        INSERT INTO comment_likes (comment_id, member_id)
        VALUES (#{commentId}, #{memberId})
    </insert>

    <delete id="deleteLike">
        DELETE FROM comment_likes
        WHERE comment_id = #{commentId} AND member_id = #{memberId}
    </delete>

    <select id="existsLike" resultType="int">
        SELECT COUNT(*) FROM comment_likes
        WHERE comment_id = #{commentId} AND member_id = #{memberId}
    </select>

    <select id="countByCommentId" resultType="int">
        SELECT COUNT(*) FROM comment_likes
        WHERE comment_id = #{commentId}
    </select>

</mapper>
