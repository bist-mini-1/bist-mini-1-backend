<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bist.mini.post.dao.LikeDao">

    <select id="countLike" resultType="int">
        SELECT COUNT(*)
        FROM post_likes
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </select>

    <insert id="insertLike">
        INSERT INTO post_likes (post_id, member_id, created_at)
        VALUES (#{postId}, #{memberId}, SYSDATE)
    </insert>

    <delete id="deleteLike">
        DELETE FROM post_likes
        WHERE post_id = #{postId} AND member_id = #{memberId}
    </delete>

    <update id="updateLikeCount">
        UPDATE posts
        SET like_count = like_count + #{diff}
        WHERE post_id = #{postId}
    </update>

</mapper>
