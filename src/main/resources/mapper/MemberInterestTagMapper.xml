<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bist.mini.member.dao.MemberInterestTagDao">

    <insert id="insertMemberInterestTags">
        INSERT ALL
        <foreach item="tagId" collection="tagIds">
            INTO member_interest_tags (
            member_id,
            tag_id,
            created_at
            ) VALUES (
            #{memberId},
            #{tagId},
            SYSDATE
            )
        </foreach>
        SELECT 1 FROM dual
    </insert>

    <select id="selectInterestTagIdsByMemberId" resultType="long">
        SELECT tag_id
        FROM member_interest_tags
        WHERE member_id = #{memberId}
        ORDER BY created_at
    </select>

    <select id="countInterestTagsByMemberId" resultType="int">
        SELECT COUNT(*)
        FROM member_interest_tags
        WHERE member_id = #{memberId}
    </select>

    <delete id="deleteByMemberId">
        DELETE FROM member_interest_tags
        WHERE member_id = #{memberId}
    </delete>

</mapper>