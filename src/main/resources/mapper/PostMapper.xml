<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bist.mini.post.dao.PostDao">

    <resultMap id="postResultMap" type="com.bist.mini.post.entity.Post">
        <id property="postId" column="post_id" />
        <result property="memberId" column="member_id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="viewCount" column="view_count" />
        <result property="likeCount" column="like_count" />
        <result property="commentCount" column="comment_count" />
        <result property="isPublic" column="is_public" />
        <result property="isTemp" column="is_temp" />
        <result property="isDeleted" column="is_deleted" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
        <result property="deletedAt" column="deleted_at" />
    </resultMap>

    <select id="findAll" resultMap="postResultMap">
        SELECT
            post_id, member_id, title, content, view_count, like_count, comment_count,
            is_public, is_temp, is_deleted, created_at, updated_at, deleted_at
        FROM posts
        WHERE is_deleted = 'N'
          AND is_temp = 'N'
        ORDER BY created_at DESC
    </select>

    <select id="findById" parameterType="long" resultMap="postResultMap">
        SELECT
            post_id, member_id, title, content, view_count, like_count, comment_count,
            is_public, is_temp, is_deleted, created_at, updated_at, deleted_at
        FROM posts
        WHERE post_id = #{param1}
          AND is_deleted = 'N'
    </select>

    <insert id="insert" parameterType="com.bist.mini.post.entity.Post" useGeneratedKeys="true" keyProperty="postId" keyColumn="post_id">
        INSERT INTO posts (
            member_id, title, content, is_public, is_temp, created_at, updated_at
        )
        VALUES (
            #{memberId}, #{title}, #{content}, #{isPublic}, #{isTemp}, SYSDATE, SYSDATE
        )
    </insert>

    <select id="findByMemberId" parameterType="long" resultMap="postResultMap">
        SELECT
            post_id, member_id, title, content, view_count, like_count, comment_count,
            is_public, is_temp, is_deleted, created_at, updated_at, deleted_at
        FROM posts
        WHERE member_id = #{param1}
          AND is_deleted = 'N'
          AND is_temp = 'N'
        ORDER BY created_at DESC
    </select>

    <select id="findTempByMemberId" parameterType="long" resultMap="postResultMap">
        SELECT
            post_id, member_id, title, content, view_count, like_count, comment_count,
            is_public, is_temp, is_deleted, created_at, updated_at, deleted_at
        FROM posts
        WHERE member_id = #{param1}
          AND is_deleted = 'N'
          AND is_temp = 'Y'
        ORDER BY updated_at DESC
    </select>

    <update id="updateViewCount" parameterType="long">
        UPDATE posts
        SET view_count = view_count + 1,
            updated_at = SYSDATE
        WHERE post_id = #{param1}
          AND is_deleted = 'N'
    </update>

    <update id="updatePost" parameterType="com.bist.mini.post.entity.Post">
        UPDATE posts
        SET title = #{title},
            content = #{content},
            is_public = #{isPublic},
            is_temp = #{isTemp},
            updated_at = SYSDATE
        WHERE post_id = #{postId}
          AND member_id = #{memberId}
          AND is_deleted = 'N'
    </update>

    <update id="softDeletePost" parameterType="map">
        UPDATE posts
        SET is_deleted = 'Y',
            deleted_at = SYSDATE,
            updated_at = SYSDATE
        WHERE post_id = #{postId}
          AND member_id = #{memberId}
          AND is_deleted = 'N'
    </update>

    <!-- 관련 테이블 소프트 삭제 쿼리 -->
    <update id="softDeleteCommentsByPostId" parameterType="long">
        UPDATE comments
        SET is_deleted = 'Y',
            deleted_at = SYSDATE,
            updated_at = SYSDATE
        WHERE post_id = #{postId}
          AND is_deleted = 'N'
    </update>

    <update id="softDeletePostTagsByPostId" parameterType="long">
        DELETE FROM post_tags
        WHERE post_id = #{postId}
    </update>

    <insert id="insertPostTag">
        INSERT INTO post_tags (
            post_id, tag_id
        ) VALUES (
            #{postId}, #{tagId}
        )
    </insert>

    <update id="softDeletePostLikesByPostId" parameterType="long">
        DELETE FROM post_likes
        WHERE post_id = #{postId}
    </update>

    <update id="softDeleteBookmarksByPostId" parameterType="long">
        DELETE FROM bookmarks
        WHERE post_id = #{postId}
    </update>

    <update id="softDeleteAttachmentsByPostId" parameterType="long">
        UPDATE attachments
        SET is_deleted = 'Y',
            deleted_at = SYSDATE
        WHERE post_id = #{postId}
          AND is_deleted = 'N'
    </update>


    <!-- 페이지네이션 쿼리 -->
    <select id="findAllWithPage" resultMap="postResultMap">
        SELECT
            post_id, member_id, title, content, view_count, like_count, comment_count,
            is_public, is_temp, is_deleted, created_at, updated_at, deleted_at
        FROM posts
        WHERE is_deleted = 'N'
          AND is_temp = 'N'
        ORDER BY created_at DESC
        OFFSET #{offset} ROWS
        FETCH NEXT #{limit} ROWS ONLY
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM posts
        WHERE is_deleted = 'N'
          AND is_temp = 'N'
    </select>

</mapper>
